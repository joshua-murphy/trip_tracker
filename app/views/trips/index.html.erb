<h1>All Trips</h1>

<% if @trips.any? %>
  <% @trips.each do |trp| %>
    <%= link_to trp.name, trip_path(trp) %>
  <% end %>
<% else %>
  <h2>No trips saved, <%= link_to "create one?", new_trip_path %></h2>
<% end %>
