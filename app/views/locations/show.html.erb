<h1><%= @location.name %></h1>

<% if @location.address %>
<%= link_to "Address", location_address_path(@location, @location.address) %>
<% else %>
<%= link_to "Add address", new_location_address_path(@location) %>
<% end %>
<!-- Edit, Delete -->